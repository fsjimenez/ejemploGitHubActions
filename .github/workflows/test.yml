name: Ejemplo CI/CD

on:
  push:

jobs: 
  inicio:
    runs-on: windows-latest
    steps:
      - name: mostrar texto
        run: echo "Hola mundo"

  build:
    runs-on: windows-latest
    needs: [inicio]
    steps:
      - name: mostrar texto
        run: echo "Construir"
        
  deploy:
    runs-on: windows-latest
    needs: [build]
    steps:
      - name: Configurar entorno de despliegue
        run: echo "Preparando entorno de despliegue"

      - name: Simular despliegue
        run: echo "Aplicación desplegada correctamente"
                